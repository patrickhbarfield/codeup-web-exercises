"use strict";

<!--Create an HTML file with only an unordered
list with the id of pets in the body. Within
an inline script declare and array called pets
with four elements. Loop through the list
and add each element as a list item to the ul.-->




var pets = ['chupacabra', 'narwal', 'torteagle', 'beta-fish']

var names = ['Shao Lin', 'Marc Rebillit', 'Jonathan Livingston Seagull', 'Samsonite'];
{
    console.log(names.length);
    console.log(names[0]);
    console.log(names[1]);
    console.log(names[2]);
    console.log(names[3]);
}
for (var i = 0; i < names.length; i++) {
    console.log('The name at index ' + i + ' is: ' + names[i]);
}
====

function petFunk(pets){
    for (let multiplier = 1; multiplier <= 10; multiplier++)
    {
        //7 * 1 =7
        console.log(num + "x" + multiplier + " = " + num * multiplier);
        console.log(num * multiplier);
    }
}
    showMultiplicationTable(7);




for (let i = numbers.length -1; i >= 0 ; i--) {
    console.log(numbers[i]);
}

let instructors = ['Casey', 'Docrob', 'Ry', 'Jay','David'];
